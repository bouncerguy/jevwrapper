# Keep actual keys on the server. Never commit them.
OPENAI_API_KEY=
TYPESAFE_API_KEY=
OPENAI_MODEL=gpt-5-mini
JEV_MODEL=jev-latest
# Required for live web calls; generate: openssl rand -hex 32
OWNER_TOKEN=
HOST=127.0.0.1
PORT=3187
BASE_PATH=/jevwrapper
MAX_REQUESTS_PER_DAY=200
